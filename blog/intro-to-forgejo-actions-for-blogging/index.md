---
title: Intro to Forgejo Actions for Blogging
description: null
date: 2026-05-13
tags:
- publish
- forgejo
- ci-pipeline
- container
---

![Pasted image 20260513170108.png](../../Attachments/Pasted%20image%2020260513170108.png)

TODO: Idea is to use containers and forgejo actions to do a bunch of automations

1. Take a commit to this blog of markdown files and inject it into the hugos git repo (convert obsidia2026-05-13T16:45:24+00:00n markdown to regular markdown too)
1. Compile the new production site to /public
1. Push prod out to github pages

They are actually working on currently overhauling their version of github pages? https://git-pages.org/

Maybe have a publish branch and main can be the draft branch? maybe serve draft as a local website deployed in a container and nginx?

This explains why docker in docker
https://forgejo.org/docs/latest/admin/actions/docker-access/#docker-in-docker

Also with v15 now it gives you part of the yaml config for the runner that will auto pair it for you.

The other question is where to pair it? In forgejo you can pair your runner at 4 different scopes to limit which repos can access the runner. You can find the settings to add a runner and view what runners available whenever you see `Actions -> Runners` in the left nav.

|Scope|Access|Description|Settings Under|
|-----|------|-----------|--------------|
|repo|`USER/REPO`|Usable only on that specific repo|Repo Settings|
|user|`USER/*`|Usable by all of the user's repos|User Settings|
|org|`ORG/*`|Usable by all of the org's repos|Org Settings|
|global|`*/*`|Usable by all repos|Site Administration|

* TODO is there more?
  Whereever you pair it will determine who has access to make workflows that run on your runner. If your runner is totally locked down and isolated or its just a personal instance then global can be nice so that you don't have to pair a new runner every time you want to play around with actions in org repos.

This is interesting: https://www.conventionalcommits.org/en/v1.0.0/

Mention:

Also a quick recommendation, I had installed my forgejo instance with sqlite as the database and it worked flawless for my own personal commiting and occasionally viewing the website usage. However, the moment I started using runners and trying to use the website at the same time, it would freeze up. I love sqlites but its more designed for integrating into apps instead of websites so it lacks support for in this case, handling parallel requests which leads to the database locking up because the running runner and database are both fighting for the database.

So unless you plan on never using actions I highly recommend using another backend like postgress. To make matters worse, Forgejo also doesn't have an official way to migrate from sqlite to postgress so it can be hard to fix after the fact. Luckly I didn't have too many repos at the time so I just fixed it by manually recreating it from scratch.

## Writing Workflows

Its a config file that defines events and some bash/shell scripts. You can even reuse scripts from other repos.

You can create one repo with workflows so you can use the same workflow from multiple workflows: https://github.com/orgs/community/discussions/175466#discussioncomment-14564408

Reusable Builtins:
https://github.com/actions/checkout#Checkout-multiple-repos-side-by-side

Reference for yaml file format:
https://forgejo.org/docs/latest/user/actions/reference/

The developer experience of this is pretty bad. The feedback loop of have to create test commits takes forever.

## Calling Workflow Dispatch API

The api we need to call for triggering a workflow to run is:

````
POST /repos/{owner}/{repo}/actions/workflows/{workflowname}/dispatches
````

https://YOUR_FORGEJO_HOST/api/swagger#/repository/DispatchWorkflow

My recommendation would be to test out the api endoint in swagger because it shows off a lot of details about the sorts of arguments you can pass, lets you test the api in browser, and generates the curl command you need to run in the workflow.

Docs how to call api endpoints: https://forgejo.org/docs/latest/user/api-usage/
Reference for apis you can call are generated by swagger https://YOUR_FORGEJO_HOST/api/swagger

Up in the top right of swagger is an Authorize button you can use to authenticate. You can test using your forgejo account or you can use the tokenAuthorizationHeaderToken option which lets you test the exact same token you'd be using in the workflow. Just remember to enter it in the format `token MY_TOKEN`.

I spent probably 30 minutes getting stuck on the fact that my workflow file was named deploy.yaml but I was posting to to a url with deploy.yml. I honestly hadn't realized yaml had two file extensions before this. Only other time I've seen that is with jpegs (.jpeg and .jpg).

## TODO Cover Creating a CI Image Yourself

You should probably try using an existing one but, its also an excuse to do another ci pipeline!!

A lot of the ci ecosystem assumes you have git and node preinstalled into the container. The docker.io/nixos/nix container doesn't so we'd have to install it each time. This container is my personal runner that I bake things into like node and git for convenance and provide hopefully decent nix command support if I want to build things with flakes.

* Probably should look into how nixos/nix is created, that would probably be better than this super jank dockerTools setup where it takes a lot of work to get nix commands to function in the container.

See `.forgejo\workflows\build.yaml` for the build script.

### Aside

You can login and upload containers to registries with podman or docker. However, both of those tools rely on a daemon in the background which, while normally fine, when running in a ci workflow to build the container it is running within a container itself so then you'd have to deal with docker in docker which requires a privileged container and so on making it a huge hassle. We can skip all that by using a dedicated tool for registries, in this case skopeo. This is a great [intro to skopeo](https://developers.redhat.com/articles/2025/09/24/skopeo-unsung-hero-linux-container-tools) if you have seen it before.
